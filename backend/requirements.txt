# ─────────────────────────────────────────────────────────────
# AI Career Intelligence System — Production Requirements
# ─────────────────────────────────────────────────────────────
# Minimal production dependencies only.
# Do NOT add personal environment packages here.
# Run: pip install -r requirements.txt
# Then: python -m spacy download en_core_web_sm
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9

# PDF parsing
pdfplumber==0.11.4
pypdf==4.3.1

# NLP — None (spaCy removed to fix Render wheel compilation issues, keyword fallback is used)

# AI / LLM
groq==0.9.0

# HTTP client (for potential outbound calls)
httpx==0.27.2

# Data validation
pydantic==2.8.2

# Environment variables
python-dotenv==1.0.1

# Utilities
python-dateutil==2.9.0
